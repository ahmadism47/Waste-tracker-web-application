package com.waste.wastTracker.model.enums;

public enum UserRole {
    ADMIN, // manage users, view all data
    DRIVER, // handles bin collection routes
    OPERATOR //  monitors bin status and assigns routes
}
